/*
 * Copyright (c) 2011-2014, fortiss GmbH.
 * Licensed under the Apache License, Version 2.0.
 *
 * Use, modification and distribution are subject to the terms specified
 * in the accompanying license file LICENSE.txt located at the root directory
 * of this software distribution. A copy is available at
 * http://chromosome.fortiss.org/.
 *
 * This file is part of CHROMOSOME.
 *
 * $Id: systemManifest.c 7769 2014-03-11 15:27:13Z geisinger $
 */

/**
 * \file
 *         Defines function for creation of component type manifest
 *         for 'system'.
 *
 * \author
 *         This file has been generated by the CHROMOSOME Modeling Tool (XMT)
 *         (fortiss GmbH).
 */

/******************************************************************************/
/***   Includes                                                             ***/
/******************************************************************************/
#include "rOSGateway/adv/system/include/systemManifest.h"

#include "../include/systemComponentWrapper.h"
#include "rOSGateway/adv/system/include/askDifferenceFunction.h"
#include "rOSGateway/adv/system/include/askDifferenceFunctionWrapper.h"
#include "rOSGateway/adv/system/include/getSumFunction.h"
#include "rOSGateway/adv/system/include/getSumFunctionWrapper.h"
#include "rOSGateway/adv/system/include/receiveDifferenceFunction.h"
#include "rOSGateway/adv/system/include/receiveDifferenceFunctionWrapper.h"
#include "rOSGateway/adv/system/include/receiveMessageStatusFunction.h"
#include "rOSGateway/adv/system/include/receiveMessageStatusFunctionWrapper.h"
#include "rOSGateway/adv/system/include/sendMessageFunction.h"
#include "rOSGateway/adv/system/include/sendMessageFunctionWrapper.h"
#include "rOSGateway/adv/system/include/systemComponent.h"
#include "rOSGateway/topic/dictionary.h"
#include "xme/core/log.h"
#include "xme/hal/include/time.h"

/******************************************************************************/
/***   Implementation                                                       ***/
/******************************************************************************/
xme_status_t
rOSGateway_adv_system_manifest_createComponentTypeManifest
(
    xme_core_componentManifest_t* componentManifest
)
{
    XME_CHECK
    (
        NULL != componentManifest,
        XME_STATUS_INVALID_PARAMETER
    );

    (void)xme_hal_mem_set(componentManifest, 0U, sizeof(xme_core_componentManifest_t));

    componentManifest->componentType = (xme_core_componentType_t)4098;
    componentManifest->componentWrapperInit = rOSGateway_adv_system_systemComponentWrapper_init;
    componentManifest->componentWrapperReceivePort = rOSGateway_adv_system_systemComponentWrapper_receivePort;
    componentManifest->componentWrapperFini = rOSGateway_adv_system_systemComponentWrapper_fini;
    componentManifest->componentInit = (xme_core_componentManifest_componentInit_t)rOSGateway_adv_system_systemComponent_init;
    (void)xme_hal_safeString_strncpy(componentManifest->name, "system", sizeof(componentManifest->name));
    componentManifest->configStructSize = sizeof(rOSGateway_adv_system_systemComponent_config_t);

    {
        uint32_t functionArrayLength = sizeof(componentManifest->functionManifests) / sizeof(componentManifest->functionManifests[0]);

        // Function 'sendMessage'
        {
            if (0U >= functionArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (0U == functionArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more functions (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                       5,
                       functionArrayLength
                    );
                }
            }
            else
            {
                xme_core_functionManifest_t* functionManifest;
                
                functionManifest = &componentManifest->functionManifests[0];
                functionManifest->functionId = (xme_core_component_functionId_t)1;
                functionManifest->wcet = xme_hal_time_timeIntervalFromMilliseconds(1ull);
                functionManifest->alphaCurve.alphaCurve = 0;
                functionManifest->completion = true;
                functionManifest->requiredPortIndicesLength = 1;
                functionManifest->requiredPortIndices[0] = (rOSGateway_adv_system_systemComponentWrapper_internalPortId_t)ROSGATEWAY_ADV_SYSTEM_SYSTEMCOMPONENTWRAPPER_PORT_MESSAGE;
                functionManifest->optionalPortIndicesLength = 0;
                functionManifest->functionInit = (xme_core_exec_initCallback_t)rOSGateway_adv_system_sendMessageFunction_init;
                functionManifest->functionFini = (xme_core_exec_finiCallback_t)rOSGateway_adv_system_sendMessageFunction_fini;
                functionManifest->functionWrapperExecute = rOSGateway_adv_system_sendMessageFunctionWrapper_execute;
            }
        }
        // Function 'receiveMessageStatus'
        {
            if (1U >= functionArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (1U == functionArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more functions (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                       5,
                       functionArrayLength
                    );
                }
            }
            else
            {
                xme_core_functionManifest_t* functionManifest;
                
                functionManifest = &componentManifest->functionManifests[1];
                functionManifest->functionId = (xme_core_component_functionId_t)2;
                functionManifest->wcet = xme_hal_time_timeIntervalFromMilliseconds(1ull);
                functionManifest->alphaCurve.alphaCurve = 0;
                functionManifest->completion = true;
                functionManifest->requiredPortIndicesLength = 1;
                functionManifest->requiredPortIndices[0] = (rOSGateway_adv_system_systemComponentWrapper_internalPortId_t)ROSGATEWAY_ADV_SYSTEM_SYSTEMCOMPONENTWRAPPER_PORT_MESSAGESTATUS;
                functionManifest->optionalPortIndicesLength = 0;
                functionManifest->functionInit = (xme_core_exec_initCallback_t)rOSGateway_adv_system_receiveMessageStatusFunction_init;
                functionManifest->functionFini = (xme_core_exec_finiCallback_t)rOSGateway_adv_system_receiveMessageStatusFunction_fini;
                functionManifest->functionWrapperExecute = rOSGateway_adv_system_receiveMessageStatusFunctionWrapper_execute;
            }
        }
        // Function 'getSum'
        {
            if (2U >= functionArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (2U == functionArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more functions (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                       5,
                       functionArrayLength
                    );
                }
            }
            else
            {
                xme_core_functionManifest_t* functionManifest;
                
                functionManifest = &componentManifest->functionManifests[2];
                functionManifest->functionId = (xme_core_component_functionId_t)3;
                functionManifest->wcet = xme_hal_time_timeIntervalFromMilliseconds(1ull);
                functionManifest->alphaCurve.alphaCurve = 0;
                functionManifest->completion = true;
                functionManifest->requiredPortIndicesLength = 2;
                functionManifest->requiredPortIndices[0] = (rOSGateway_adv_system_systemComponentWrapper_internalPortId_t)ROSGATEWAY_ADV_SYSTEM_SYSTEMCOMPONENTWRAPPER_PORT_GETSUM;
                functionManifest->requiredPortIndices[1] = (rOSGateway_adv_system_systemComponentWrapper_internalPortId_t)ROSGATEWAY_ADV_SYSTEM_SYSTEMCOMPONENTWRAPPER_PORT_SENDSUM;
                functionManifest->optionalPortIndicesLength = 0;
                functionManifest->functionInit = (xme_core_exec_initCallback_t)rOSGateway_adv_system_getSumFunction_init;
                functionManifest->functionFini = (xme_core_exec_finiCallback_t)rOSGateway_adv_system_getSumFunction_fini;
                functionManifest->functionWrapperExecute = rOSGateway_adv_system_getSumFunctionWrapper_execute;
            }
        }
        // Function 'askDifference'
        {
            if (3U >= functionArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (3U == functionArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more functions (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                       5,
                       functionArrayLength
                    );
                }
            }
            else
            {
                xme_core_functionManifest_t* functionManifest;
                
                functionManifest = &componentManifest->functionManifests[3];
                functionManifest->functionId = (xme_core_component_functionId_t)4;
                functionManifest->wcet = xme_hal_time_timeIntervalFromMilliseconds(1ull);
                functionManifest->alphaCurve.alphaCurve = 0;
                functionManifest->completion = true;
                functionManifest->requiredPortIndicesLength = 1;
                functionManifest->requiredPortIndices[0] = (rOSGateway_adv_system_systemComponentWrapper_internalPortId_t)ROSGATEWAY_ADV_SYSTEM_SYSTEMCOMPONENTWRAPPER_PORT_GETDIFFERENCE;
                functionManifest->optionalPortIndicesLength = 0;
                functionManifest->functionInit = (xme_core_exec_initCallback_t)rOSGateway_adv_system_askDifferenceFunction_init;
                functionManifest->functionFini = (xme_core_exec_finiCallback_t)rOSGateway_adv_system_askDifferenceFunction_fini;
                functionManifest->functionWrapperExecute = rOSGateway_adv_system_askDifferenceFunctionWrapper_execute;
            }
        }
        // Function 'receiveDifference'
        {
            if (4U >= functionArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (4U == functionArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more functions (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                       5,
                       functionArrayLength
                    );
                }
            }
            else
            {
                xme_core_functionManifest_t* functionManifest;
                
                functionManifest = &componentManifest->functionManifests[4];
                functionManifest->functionId = (xme_core_component_functionId_t)5;
                functionManifest->wcet = xme_hal_time_timeIntervalFromMilliseconds(1ull);
                functionManifest->alphaCurve.alphaCurve = 0;
                functionManifest->completion = true;
                functionManifest->requiredPortIndicesLength = 1;
                functionManifest->requiredPortIndices[0] = (rOSGateway_adv_system_systemComponentWrapper_internalPortId_t)ROSGATEWAY_ADV_SYSTEM_SYSTEMCOMPONENTWRAPPER_PORT_RECEIVEDDIFFERENCE;
                functionManifest->optionalPortIndicesLength = 0;
                functionManifest->functionInit = (xme_core_exec_initCallback_t)rOSGateway_adv_system_receiveDifferenceFunction_init;
                functionManifest->functionFini = (xme_core_exec_finiCallback_t)rOSGateway_adv_system_receiveDifferenceFunction_fini;
                functionManifest->functionWrapperExecute = rOSGateway_adv_system_receiveDifferenceFunctionWrapper_execute;
            }
        }
    }

    {
        uint32_t portArrayLength;
        portArrayLength = sizeof(componentManifest->portManifests) / sizeof(componentManifest->portManifests[0]);

        // Subscription 'messageStatus'
        {
            if (0 >= portArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (0 == portArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more ports (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                        6,
                        portArrayLength
                    );
                }
            }
            else
            {
                xme_core_componentPortManifest_t* portManifest;
            
                portManifest = &componentManifest->portManifests[0];
                portManifest->portType = XME_CORE_COMPONENT_PORTTYPE_DCC_SUBSCRIPTION;
                portManifest->topic = ROSGATEWAY_TOPIC_STATUS; 
                portManifest->lowerConnectionBound = 0u;
                portManifest->upperConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_UNBOUNDED;
                portManifest->queueSize = 1;
                portManifest->persistent = false;
        
                portManifest->attrSet = XME_CORE_ATTRIBUTE_EMPTY_ATTRIBUTE_SET;
            }
        }
        // Subscription 'getSum'
        {
            if (1 >= portArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (1 == portArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more ports (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                        6,
                        portArrayLength
                    );
                }
            }
            else
            {
                xme_core_componentPortManifest_t* portManifest;
            
                portManifest = &componentManifest->portManifests[1];
                portManifest->portType = XME_CORE_COMPONENT_PORTTYPE_RR_REQUEST_HANDLER;
                portManifest->topic = ROSGATEWAY_TOPIC_SUMREQUEST; 
                portManifest->lowerConnectionBound = 0u;
                portManifest->upperConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_UNBOUNDED;
                portManifest->queueSize = 1;
                portManifest->persistent = false;
        
                portManifest->attrSet = XME_CORE_ATTRIBUTE_EMPTY_ATTRIBUTE_SET;
            }
        }
        // Subscription 'receivedDifference'
        {
            if (2 >= portArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (2 == portArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more ports (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                        6,
                        portArrayLength
                    );
                }
            }
            else
            {
                xme_core_componentPortManifest_t* portManifest;
            
                portManifest = &componentManifest->portManifests[2];
                portManifest->portType = XME_CORE_COMPONENT_PORTTYPE_RR_RESPONSE_HANDLER;
                portManifest->topic = ROSGATEWAY_TOPIC_DIFFERENCERESPONSE; 
                portManifest->lowerConnectionBound = 0u;
                portManifest->upperConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_UNBOUNDED;
                portManifest->queueSize = 1;
                portManifest->persistent = false;
        
                portManifest->attrSet = XME_CORE_ATTRIBUTE_EMPTY_ATTRIBUTE_SET;
            }
        }
        // Publication 'message'
        {
            if (3 >= portArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (3 == portArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more ports (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                        6,
                        portArrayLength
                    );
                }
            }
            else
            {
                xme_core_componentPortManifest_t* portManifest;
            
                portManifest = &componentManifest->portManifests[3];
                portManifest->portType = XME_CORE_COMPONENT_PORTTYPE_DCC_PUBLICATION;
                portManifest->topic = ROSGATEWAY_TOPIC_MESSAGE; 
                portManifest->lowerConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_INVALID;
                portManifest->upperConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_INVALID;
                portManifest->queueSize = 1;
                portManifest->persistent = false;
        
                portManifest->attrSet = XME_CORE_ATTRIBUTE_EMPTY_ATTRIBUTE_SET;
                
            }
        }
        // Publication 'sendSum'
        {
            if (4 >= portArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (4 == portArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more ports (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                        6,
                        portArrayLength
                    );
                }
            }
            else
            {
                xme_core_componentPortManifest_t* portManifest;
            
                portManifest = &componentManifest->portManifests[4];
                portManifest->portType = XME_CORE_COMPONENT_PORTTYPE_RR_RESPONSE_SENDER;
                portManifest->topic = ROSGATEWAY_TOPIC_SUMRESPONSE; 
                portManifest->lowerConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_INVALID;
                portManifest->upperConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_INVALID;
                portManifest->queueSize = 1;
                portManifest->persistent = false;
        
                portManifest->attrSet = XME_CORE_ATTRIBUTE_EMPTY_ATTRIBUTE_SET;
                
            }
        }
        // Publication 'getDifference'
        {
            if (5 >= portArrayLength) // Check generated by tool (which does not know about the array size)
            {
                if (5 == portArrayLength) // Only trigger warning once
                {
                    XME_LOG
                    (
                        XME_LOG_WARNING,
                        "%s:%d Component type 'system' defines more ports (%d) than can be stored in the manifest data structure (%d).\n",
                        __FILE__,
                        __LINE__,
                        6,
                        portArrayLength
                    );
                }
            }
            else
            {
                xme_core_componentPortManifest_t* portManifest;
            
                portManifest = &componentManifest->portManifests[5];
                portManifest->portType = XME_CORE_COMPONENT_PORTTYPE_RR_REQUEST_SENDER;
                portManifest->topic = ROSGATEWAY_TOPIC_DIFFERENCEREQUEST; 
                portManifest->lowerConnectionBound = 0u;
                portManifest->upperConnectionBound = XME_CORE_COMPONENT_CONNECTIONBOUND_UNBOUNDED;
                portManifest->queueSize = 1;
                portManifest->persistent = false;
        
                portManifest->attrSet = XME_CORE_ATTRIBUTE_EMPTY_ATTRIBUTE_SET;
                
            }
        }
    }
    return XME_STATUS_SUCCESS;
}
