This package wrappes the external c++ library openGM to use it in other packages. This library is a header-only library so it doesn't needs to get build.
See the Makefile.tarball for further information.
