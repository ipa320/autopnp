Generic C implementations of functionality for which an architecture
specific port is available for some architectures (read: assembler).

This folder intentionally does not contain a CMakeLists.txt since its
files are intented to be included by the correspondig architectures.
